apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ans-semaphore.fullname" . }}-test-connection"
  labels:
    {{- include "ans-semaphore.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ans-semaphore.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
