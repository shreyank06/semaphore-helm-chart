apiVersion: v1
kind: Service
metadata:
  name: {{ include "ans-semaphore.fullname" . }}
  labels:
    {{- include "ans-semaphore.labels" . | nindent 4 }}
spec:
  type: {{ .Values.semaphore.service.type }}
  ports:
    - port: {{ .Values.semaphore.service.port }}
      targetPort: http
      protocol: TCP
      name: http
      {{- if eq .Values.semaphore.service.type "NodePort" }}
      nodePort: {{ .Values.semaphore.service.nodePort }}
      {{- end }}
  selector:
    {{- include "ans-semaphore.selectorLabels" . | nindent 4 }}
