apiVersion: v1
kind: Service
metadata:
  name: {{ include "ans-semaphore.fullname" . }}
  labels:
    {{- include "ans-semaphore.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ans-semaphore.selectorLabels" . | nindent 4 }}
